import { describe, it, expect } from 'vitest';
import yd_array_paging from '../../lib/array/paging.js';

describe('yd_array_paging', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
