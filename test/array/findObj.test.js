import { describe, it, expect } from 'vitest';
import yd_array_findObj from '../../lib/array/findObj.js';

describe('yd_array_findObj', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});