import { describe, it, expect } from 'vitest';
import yd_array_keyBy from '../../lib/array/keyBy.js';

describe('yd_array_keyBy', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
