import { describe, it, expect } from 'vitest';
import yd_array_sortBy from '../../lib/array/sortBy.js';

describe('yd_array_sortBy', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});