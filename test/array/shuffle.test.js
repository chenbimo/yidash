import { describe, it, expect } from 'vitest';
import yd_array_shuffle from '../../lib/array/shuffle.js';

describe('yd_array_shuffle', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});