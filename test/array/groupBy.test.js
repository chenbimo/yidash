import { describe, it, expect } from 'vitest';
import yd_array_groupBy from '../../lib/array/groupBy.js';

describe('yd_array_groupBy', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});