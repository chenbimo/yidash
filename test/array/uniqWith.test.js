import { describe, it, expect } from 'vitest';
import yd_array_uniqWith from '../../lib/array/uniqWith.js';

describe('yd_array_uniqWith', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
