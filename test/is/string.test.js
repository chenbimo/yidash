import { describe, it, expect } from 'vitest';
import yd_is_string from '../../lib/is/string.js';

describe('yd_is_string', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
