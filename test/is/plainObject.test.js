import { describe, it, expect } from 'vitest';
// import yd_is_plainObject from '../../lib/is/plainObject.js';

describe('yd_is_plainObject', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
