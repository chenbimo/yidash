import { describe, it, expect } from 'vitest';
import yd_is_integer from '../../lib/is/integer.js';

describe('yd_is_integer', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});