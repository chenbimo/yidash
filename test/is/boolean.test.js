import { describe, it, expect } from 'vitest';
import yd_is_boolean from '../../lib/is/boolean.js';

describe('yd_is_boolean', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
