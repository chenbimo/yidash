import { describe, it, expect } from 'vitest';
import yd_is_mobile from '../../lib/is/mobile.js';

describe('yd_is_mobile', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
