import { describe, it, expect } from 'vitest';
// import yd_is_number from '../../lib/is/number.js';

describe('yd_is_number', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
