import { describe, it, expect } from 'vitest';
import yd_is_undefined from '../../lib/is/undefined.js';

describe('yd_is_undefined', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
