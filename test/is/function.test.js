import { describe, it, expect } from 'vitest';
// import yd_is_function from '../../lib/is/function.js';

describe('yd_is_function', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
