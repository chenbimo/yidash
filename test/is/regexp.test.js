import { describe, it, expect } from 'vitest';
import yd_is_regexp from '../../lib/is/regexp.js';

describe('yd_is_regexp', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
