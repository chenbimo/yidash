import { describe, it, expect } from 'vitest';
// import yd_is_weakMap from '../../lib/is/weakMap.js';

describe('yd_is_weakMap', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
