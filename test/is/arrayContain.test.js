import { describe, it, expect } from 'vitest';
import yd_is_arrayContain from '../../lib/is/arrayContain.js';

describe('yd_is_arrayContain', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});