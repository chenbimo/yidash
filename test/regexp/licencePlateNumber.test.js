import { describe, expect, it } from 'vitest';
// import yd_regexp_licencePlateNumber from '../../lib/regexp/licencePlateNumber.js';

describe('yd_regexp_licencePlateNumber', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    })
});
