import { describe, it, expect } from 'vitest';
import yd_data_randomSelect from '../../lib/data/randomSelect.js';

describe('yd_data_randomSelect', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});