import { describe, it, expect } from 'vitest';
import yd_string_randomString from '../../lib/string/randomString.js';

describe('yd_string_randomString', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});