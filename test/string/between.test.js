import { describe, it, expect } from 'vitest';
import yd_string_between from '../../lib/string/between.js';

describe('yd_string_between', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});