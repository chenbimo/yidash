import { describe, it, expect } from 'vitest';
import yd_string_reverse from '../../lib/string/reverse.js';

describe('yd_string_reverse', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});