import { describe, it, expect } from 'vitest';
import yd_string_generateRandomString from '../../lib/string/generateRandomString.js';

describe('yd_string_generateRandomString', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
