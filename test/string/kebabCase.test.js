import { describe, it, expect } from 'vitest';
import yd_string_kebabCase from '../../lib/string/kebabCase.js';

describe('yd_string_kebabCase', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
