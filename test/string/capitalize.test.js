import { describe, it, expect } from 'vitest';
// import yd_string_capitalize from '../../lib/string/capitalize.js';

describe('yd_string_capitalize', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
