import { describe, it, expect } from 'vitest';
import yd_string_snakeCase from '../../lib/string/snakeCase.js';

describe('yd_string_snakeCase', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
