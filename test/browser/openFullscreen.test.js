import { describe, it, expect } from 'vitest';
import yd_browser_openFullscreen from '../../lib/browser/openFullscreen.js';

describe('yd_browser_openFullscreen', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
