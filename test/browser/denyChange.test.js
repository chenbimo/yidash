import { describe, it, expect } from 'vitest';
import yd_browser_denyChange from '../../lib/browser/denyChange.js';

describe('yd_browser_denyChange', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
