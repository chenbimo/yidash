import { describe, it, expect } from 'vitest';
import yd_helper_debounce from '../../lib/helper/debounce.js';

describe('yd_helper_debounce', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});