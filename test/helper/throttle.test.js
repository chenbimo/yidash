import { describe, it, expect } from 'vitest';
import yd_helper_throttle from '../../lib/helper/throttle.js';

describe('yd_helper_throttle', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});