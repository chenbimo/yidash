import { describe, it, expect } from 'vitest';
import yd_internal_getValueType from '../../lib/internal/getValueType.js';

describe('yd_internal_getValueType', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
