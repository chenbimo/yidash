import { describe, it, expect } from 'vitest';
import yd_faker_mobileNumber from '../../lib/faker/mobileNumber.js';

describe('yd_faker_mobileNumber', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});