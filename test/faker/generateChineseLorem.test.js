import { describe, it, expect } from 'vitest';
import yd_faker_generateChineseLorem from '../../lib/faker/generateChineseLorem.js';

describe('yd_faker_generateChineseLorem', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
