import { describe, it, expect } from 'vitest';
import yd_faker_generateWordLorem from '../../lib/faker/generateWordLorem.js';

describe('yd_faker_generateWordLorem', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});