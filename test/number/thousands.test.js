import { describe, it, expect } from 'vitest';
import yd_number_thousands from '../../lib/number/thousands.js';

describe('yd_number_thousands', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
