import { describe, it, expect } from 'vitest';
// import yd_address_areaMatch from '../../lib/address/areaMatch.js';

describe('yd_address_areaMatch', () => {
    it('默认单测', () => {
        expect(true).toBe(true);
    });
});
